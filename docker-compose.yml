version: '3'
services:
  gitpulse:
    build: .
    ports:
      - "4000:4000"
    env_file:
      - .env
    restart: always
    logging:
      driver: "gelf"
      options:
        gelf-address: "udp://192.168.203.34:5044"
        tag: "gitpulse"