version: '3'
services:
  gitpulse:
    build: .
    ports:
      - "4000:4000"
    env_file:
      - .env
    restart: always
    logging:
      driver: "gelf"
      options:
        gelf-address: "ip:port"
        tag: "gitpulse"